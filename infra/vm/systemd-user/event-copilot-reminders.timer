# Fires the reminder sweep every 5 minutes. Enable with:
#   systemctl --user enable --now event-copilot-reminders.timer
[Unit]
Description=Event Copilot — reminder sweep every 5 min

[Timer]
OnBootSec=2min
OnUnitActiveSec=5min
AccuracySec=30s
Persistent=true

[Install]
WantedBy=timers.target
