# Fires the reminder sweep every minute. Enable with:
#   systemctl --user enable --now event-copilot-reminders.timer
[Unit]
Description=Event Copilot — reminder sweep every 1 min

[Timer]
OnBootSec=1min
OnUnitActiveSec=1min
AccuracySec=15s
Persistent=true

[Install]
WantedBy=timers.target
