# workers/ — Cloudflare Workers

Workers deploy via **`wrangler`** (not Vercel, not containers). This is the **only** place allowed to use D1 bindings. Currently only `data-api/`.

See `data-api/AGENTS.md`.
